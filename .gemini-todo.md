# Implementation: Robust A2A Provider

## 1. Robust Server Management (Zombie Prevention)
- [ ] Add `uncaughtException` and `unhandledRejection` listeners for cleanup in `src/server-manager.ts`
- [ ] Implement port conflict detection and automatic process killing in `src/server-manager.ts`
- [ ] Enhance `stopServer()` to ensure all child processes are definitely terminated

## 2. Externalized & Dynamic Routing
- [ ] Extend `src/config.ts` to support loading routing config from `a2a-config.json`
- [ ] Implement hot-reloading using `fs.watch` in `src/config.ts`
- [ ] Update `src/router.ts` to use dynamic routing table instead of hardcoded logic
- [ ] Add a sample `a2a-config.json.example` file

## 3. Verification & Testing
- [ ] Verify existing tests still pass
- [ ] Create a new integration test for hot-reloading routing config
- [ ] Manually verify zombie process prevention by simulating a crash
